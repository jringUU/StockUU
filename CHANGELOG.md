# 專案修改異動記錄檔 (CHANGELOG)

本檔案記錄本專案（MoneyDJ 智慧選股與 CSV 匯出系統）的所有異動歷程。

---

## 🔒 開發維護規範 (SOP)

1. **修改前強制備份**：
   - 每次對專案檔案進行任何新增、修改或刪除前，必須先將現行版本複製保存至 `backups/backup_YYYYMMDD_HHMMSS/` 備份區。
2. **同步更新異動記錄**：
   - 每次修改完成後，必須在此檔案（`CHANGELOG.md`）的最上方追加最新異動條目，載明：
     - 日期與時間
     - 異動目的 / 需求來源
     - 異動檔案清單
     - 詳細修改內容說明
     - 對應之備份資料夾路徑

---

## 📝 異動歷史記錄

### [2026-09-15 08:44:00] - 實作 Vercel 雲端 Serverless 架構與本機 100% 靜默無黑視窗啟動
- **異動目的**：
  依使用者指示「先幫我改 讓我本機可以執行 後續我再放到 github 然後 vercel」，建置 Vercel 雲端無伺服器即時查詢架構（`api/screen.py` + `vercel.json`），同時將本機端改造為 100% 靜默無黑視窗啟動（`start.vbs` + `stop.bat`），前後端統一呼叫 `/api/screen`，徹底解決黑視窗困擾與瀏覽器 CORS 阻擋問題。
- **異動檔案**：
  - `[新增]` `api/screen.py`（Vercel 雲端 Serverless Function，以 Python 原生即時向 MoneyDJ 發送查詢、解碼 Big5 並運算當沖 6 大指標與隔日沖對策）
  - `[新增]` `vercel.json`（配置 Vercel 路由轉發與 API 規則）
  - `[新增]` `start.vbs`（利用 Windows 原生 WScript 實現 100% 靜默啟動，完全消除黑色控制台視窗）
  - `[新增]` `stop.bat`（一鍵安全停止本機背景服務工具）
  - `[修改]` `start.bat`（改為呼叫 `start.vbs` 進行無感啟動並開啟瀏覽器）
  - `[修改]` `push_to_github.bat`（增強自動 git add、commit 與 push，一鍵推送到 GitHub 觸發 Vercel 自動部署）
  - `[修改]` `app.js`（無縫相容 `/api/screen` 與前端直連備援，本機與 Vercel 雲端共用同套邏輯）
  - `[修改]` `index.html`（更新狀態指示為「動態選股服務就緒」）
  - `[修改]` `README.md`（完整撰寫本機無黑視窗使用手冊與 Vercel 30 秒一鍵部署指南）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260915_084128/` (最新) 與 `backups/backup_20260915_080919/` (次新)
- **詳細修改內容**：
  1. **本機零黑視窗靜默執行**：
     - 使用者只要雙擊 `start.bat`（或 `start.vbs`），系統在背景以完全隱藏視窗（WindowStyle 0）啟動後端，並立即在預設瀏覽器開啟 `http://localhost:8080/`，完全沒有任何黑色視窗彈出。
  2. **Vercel 雲端 Serverless 支援**：
     - 建立 `api/screen.py`，未來推送到 GitHub 並連結 Vercel 後，Vercel 雲端函數會在每次點擊「開始篩選」時以 50ms 極速喚醒，直連 MoneyDJ 原站取得最新即時結果。
  3. **自動相容架構**：
     - 前端呼叫 `/api/screen`，不論是在本機跑還是在 Vercel 雲端跑，行為完全一致、穩定且免受瀏覽器 CORS 限制。

### [2026-09-15 08:12:00] - 全面改版為純前端直連 MoneyDJ 原站即時篩選與數據整理架構（完全免本機伺服器）
- **異動目的**：
  依使用者明確指令「觸發開始篩選的按鈕應該是直接向moneyDJ網站索取資料回來整理。不要自己編寫server端」，徹底移除本機 PowerShell 伺服器端（`server.ps1` 與 8080 端口）的依賴，全面改為純前端 JavaScript 直接向 MoneyDJ 官方伺服器發送篩選請求、原生解碼 Big5、正規化解析 HTML 表格，並於前端執行當沖 6 大指標量化診斷與隔日沖對策。
- **異動檔案**：
  - `[修改]` `app.js`（新增 `fetchMoneyDJDirect()` 直接向 MoneyDJ 請求、`parseMoneyDJHtml()` 原生 Big5 解碼與表格整理、`computeDayTradingIndicators()` 純前端當沖 6 大指標與隔日沖量化運算法；重構 `runScreening()` 改為純前端流程）
  - `[修改]` `index.html`（導航列狀態更新為「🌐 MoneyDJ 原站直連模式」）
  - `[修改]` `start.bat`（簡化為單行直接以預設瀏覽器開啟 `index.html`，不再啟動 `server.ps1`）
  - `[修改]` `README.md`（更新使用說明為純前端原站直連模式，免伺服器）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260915_080919/` (最新) 與 `backups/backup_20260915_080129/` (次新)
- **詳細修改內容**：
  1. **免除伺服器端（Zero-Backend）架構設計**：
     - 使用者雙擊 `start.bat` 或直接開啟 `index.html` 即可立即使用，無須啟動任何本機 PowerShell 伺服器，無連接埠占用或背景程序管理問題。
  2. **純前端 MoneyDJ 原站即時連線與解碼**：
     - 在 `app.js` 實作多路備援 CORS 閘道直接索取 MoneyDJ 原站篩選網頁（`zkResult.asp`）。
     - 使用瀏覽器原生 `TextDecoder('big5')` 完整正確解碼台股繁體中文個股名稱與技術指標，避免中文亂碼問題。
     - 使用原生 `DOMParser` 解析 `tr.zkt2R` 表格，提取代號、名稱、收盤、漲跌、DIF(週)、MACD(週)、主力買超、營收成長率。
  3. **純前端 Stock01 當沖量化引擎**：
     - 將原先由 `server.ps1` 計算的 6 大指標（均價線、江波圖、K線形態、內外盤比、差異分析、券商分點）與隔日沖風險評估演算法完整移植至 `app.js` 純 JavaScript 運算。

### [2026-09-09 20:46:00] - 支援部署至 GitHub (StockUU) 與配置 GitHub Actions 每日晚上 10 點自動選股郵件日報
- **異動目的**：依使用者需求「把程式放到 https://github.com/jringUU/StockUU 並做每天晚上10點自動執行並匯出總結文件寄到jringyou@gmail.com，我的專案是public，我的mail在裏面會不會有資安問題」，建立完整的 GitHub 倉庫架構、GitHub Actions 雲端排程工作流（每日 22:00 自動無人值守執行）、產出高質感 HTML 總結與 CSV 附檔寄送至信箱，並全面採用 GitHub Secrets 零洩漏資安架構杜絕任何密碼外洩風險。
- **異動檔案**：
  - `[新增]` `.github/workflows/daily_report.yml`（配置 GitHub Actions 每日 UTC 14:00 / 台灣 22:00 定時排程與手動一鍵觸發工作流）
  - `[新增]` `scripts/daily_report.py`（Python 自動化選股、產生 HTML 總結信件、產出 UTF-8 BOM CSV 附檔並以 Gmail SMTP 安全發信）
  - `[新增]` `scripts/daily_report.ps1`（PowerShell 本機/雲端跨平台選股日報與發信腳本）
  - `[新增]` `run_daily_report.bat`（本機一鍵雙擊執行選股日報產出工具）
  - `[新增]` `.gitignore`（自動忽略 `backups/`, `output/`, `temp/`, `*.log`, `.system_generated/` 等，保持代碼純淨）
  - `[修改]` `README.md`（更新完整的 GitHub Actions 設定說明、Google 應用程式密碼申請 4 步驟與 GitHub Secrets 資安防護指引）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_204034/` (最新) 與 `backups/backup_20260909_202055/` (次新)
- **詳細修改內容**：
  1. **公開儲存庫零外洩資安設計**：
     - 在程式碼中完全不硬編碼任何個人 Email 或 Gmail 密碼，全部透過 GitHub Secrets（`GMAIL_USER`, `GMAIL_APP_PASSWORD`, `RECEIVER_EMAIL`）動態安全傳遞。
     - 任何瀏覽公開 GitHub 儲存庫的訪客完全無法看見或爬取信箱與憑證，日誌輸出亦自動將憑證遮蔽為 `***`，徹底消除公開專案的資安顧慮。
  2. **每日晚上 10 點自動化選股與發信流程**：
     - 設定 Cron 排程 `0 14 * * *`（台灣時間 22:00），由 GitHub 雲端主機自動執行 MoneyDJ 三大核心條件篩選（週 MACD 金叉、20日主力買超、3月營收月增）。
     - 生成結構化 HTML 郵件，內含關鍵多頭指標卡片、個股明細表格與波段操作紀律指引。
     - 同步生成帶有 UTF-8 BOM 的 Excel 相容 CSV 檔案作為郵件附件。
  3. **本機獨立執行與跨平台相容性**：
     - 新增 `run_daily_report.bat` 與 `scripts/daily_report.ps1`，本機無需安裝 Python 即可隨時單鍵執行產出 HTML 與 CSV。
  4. **Git 版本控制初始化與備份守則維護**：
     - 本地初始化 Git 倉庫，配置遠端 `https://github.com/jringUU/StockUU.git` 並設定主分支為 `main`。
     - 嚴格遵守規範建立時間戳記備份 `backup_20260909_204034`，備份目錄僅保留最新 2 份。

### [2026-09-09 20:21:00] - 點擊開始篩選時將波段勾選預設改為未勾選
- **異動目的**：依使用者需求「當按下開始篩選時把波段的勾選改成未勾選」，將篩選結果出爐時的預設行為由「全選所有符合標的進入波段」改為「全部預設未勾選」，由使用者手動自選標的再納入「波段手法指標」模組進行深度追蹤與評級。
- **異動檔案**：
  - `[修改]` `index.html`（移除 `#checkAllSwing` 表頭核取方塊的預設 `checked` 屬性）
  - `[修改]` `app.js`（在 `runScreening()` 中清空 `selectedSwingCodes.clear()`，確保每次篩選結果產出時所有個股波段核取方塊皆為未勾選狀態，且同步更新表頭波段核取狀態為未選與統計為 `0 檔波段`）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_202055/` (最新) 與 `backups/backup_20260909_201512/` (次新)
- **詳細修改內容**：
  1. **預設勾選行為調整**：
     - 修改 `app.js` 之 `runScreening()`，將原本篩選完成後的自動全選 `if (selectedSwingCodes.size === 0) { allStocks.forEach(...) }` 移除，改為執行 `selectedSwingCodes.clear()`。
     - 每次點擊「開始篩選」後，智慧選股主表格的「波段」欄位一律預設為未勾選，避免一鍵將大量個股直接全數灌入波段清單。
  2. **表頭與徽章連動狀態同步**：
     - 表頭「波段」全選 Checkbox 初始與篩選後均維持未勾選（`checked = false`），波段分頁標籤即時顯示 `0 檔波段`。
     - 使用者可依需求手動勾選欲操作個股，或點擊表頭「波段」核取方塊快速切換全選/全取消。
  3. **備份與規範維護**：
     - 修改前已建立時間戳記備份目錄 `backups/backup_20260909_202055/`，備份核心檔案並嚴格保留最新 2 份備份。

### [2026-09-09 20:17:00] - 籌碼面（主力買超）與營收獲利面（營收月增）新增獨立勾選核取方塊並與 MoneyDJ 條件全面連動
- **異動目的**：依使用者需求「近 日券商主力買超增加可以勾選的選項、近 月平均月營收成長增加可以勾選的選項、有勾選視為正式條件,按下開始篩選把條件送到MoneyDJ」，將技術面、籌碼面、營收獲利面三大核心篩選條件全面改為獨立核取方塊（Checkbox），有勾選才納入正式查詢字串傳送給 MoneyDJ，支援使用者自由排列組合任何 1~3 項條件。
- **異動檔案**：
  - `[修改]` `index.html`（在「籌碼面」與「營收獲利面」卡片前方新增自訂核取方塊 `#condChipEnable` 與 `#condRevEnable`）
  - `[修改]` `style.css`（新增 `.condition-card.inactive` 停用時半透明柔化樣式，突顯啟用/未啟用狀態）
  - `[修改]` `app.js`（即時連動勾選狀態與輸入框啟用/停用；篩選與 CSV 匯出時精準傳遞 `chip_enable` 與 `rev_enable`，未勾選時動態變更表頭為「未勾選」）
  - `[修改]` `server.ps1`（後端全面支援 `chip_enable` 與 `rev_enable` 參數解析，未勾選之條件完全不送出 `x@370` 或 `x@5720` 至 MoneyDJ）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_201512/` (最新) 與 `backups/backup_20260909_201028/` (次新)
- **詳細修改內容**：
  1. **前台介面全面統一三合一勾選結構**：
     - 在「籌碼面」卡片前加入藍色自訂勾選核取方塊（`#condChipEnable`，預設勾選）。
     - 在「營收獲利面」卡片前加入藍色自訂勾選核取方塊（`#condRevEnable`，預設勾選）。
     - 當取消勾選時，卡片自動呈現半透明反灰停用樣式（`.inactive`），輸入框自動進入 `disabled` 狀態；勾選時瞬間點亮並恢復可編輯狀態。
  2. **條件整合與 MoneyDJ 查詢連動**：
     - 點擊「開始篩選」時，若三項皆未勾選會彈出防呆提示，否則只將「有勾選」的條件組裝為 MoneyDJ 語法（例如：只選主力買超即傳送 `A=x@370...`，只選技術+營收即傳送 `A=x@1301;x@5720...`）。
  3. **後台與 CSV 匯出相容支援**：
     - `server.ps1` 與 `exportToCSV` 自動偵測勾選狀態，若某條件未勾選，CSV 表頭自動標示為「未勾選」，資料欄位賦予 `"-"`。
  4. **備份與規範維護**：
     - 自動建立最新備份 `backup_20260909_201512`，並嚴格維持僅保留最新與次新（共 2 個）資料夾。

### [2026-09-09 20:13:00] - 修復取消勾選週 MACD 時伺服器欄位解析失敗導致查無資料之重大 Bug
- **異動目的**：修復當使用者取消勾選「DIF值 向上突破 MACD（週）」條件時，MoneyDJ 回傳的 HTML 表格缺少 DIF/MACD 兩欄（欄位數由 8 欄降為 6 欄），導致後端 `server.ps1` 原先固定要求 8 欄判定（`$tdMatches.Count -ge 8`）全數失敗並回傳 0 檔的問題。
- **異動檔案**：
  - `[修改]` `server.ps1`（改為依勾選條件動態對應欄位索引，支援僅選籌碼/營收等自訂組合；TWSE MIS API 批次分包避免長度限制）
  - `[修改]` `app.js`（在未勾選週 MACD 時，走勢欄與波段表格優雅顯示「未納入週MACD」，多頭評級不強制要求週金叉）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_201028/` (最新) 與 `backups/backup_20260909_194827/` (次新)
- **詳細修改內容**：
  1. **伺服器端動態欄位解析修復**：
     - 在 `server.ps1` 的 `Fetch-MoneyDJData` 中，根據 `$hasDif`、`$hasChip`、`$hasRev` 動態指派 MoneyDJ 回傳之 `<td>` 索引。
     - 最低欄位要求降為基礎行情 4 欄（股票、收盤、漲跌、漲跌幅），若未勾選週 MACD 則其欄位安全賦予 `"-"`，徹底解決取消勾選後 116 檔資料全部被過濾成 0 檔的異常。
  2. **TWSE MIS API 分批請求最佳化**：
     - 當篩選條件寬鬆導致標的數較多（如 100+ 檔）時，自動以每組 30 筆代碼分批查詢 MIS API，防止 URL 總長過長被 TWSE 伺服器拒絕。
  3. **前端介面在無 MACD 資料時之相容展示**：
     - `app.js` 中的 `generateMacd12Sparkline` 與 `renderWaveStrategyTable` 在遇到 `difWeek` 為 `"-"` 時，自動切換至「未納入週MACD」提示，計算強勢多頭時自動僅依籌碼與營收進行判定。
  4. **備份與規範維護**：
     - 自動建立最新備份 `backup_20260909_201028`，並嚴格維持僅保留最新與次新（共 2 個）資料夾。

### [2026-09-09 19:49:00] - 畫面各表格全面增加與強化 rev (營收月增成長率) 標籤與多頭門檻評級指標
- **異動目的**：依使用者需求「畫面表格增加rev值」，在四大核心功能表格中全面加強營收成長率（rev值）之標示與狀態徽章，特別在「波段手法指標」表格中明確標註 `🔥 rev>2% (強勢)` 與 `🟡 rev 1~2% (穩健)`，讓使用者直觀理解「強勢多頭評級」的判定依據。
- **異動檔案**：
  - `[修改]` `index.html`（「智慧選股」表頭增列 `營收成長 rev(%)`；「波段手法指標」表頭增列 `④營收月增 (rev值)`）
  - `[修改]` `app.js`（波段手法表格動態計算並展示 `rev>2% 強勢` / `rev 1~2% 穩健` 評級；智慧選股表格標示 `rev值`；當沖與隔日沖表格增加 `rev:+X%` 迷你標籤）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_194827/` (最新) 與 `backups/backup_20260909_193235/` (次新)
- **詳細修改內容**：
  1. **波段手法指標表格增加 rev值 評級狀態**：
     - 在「④營收月增 (rev值)」欄位中，除呈現月增率數值外，依據多頭門檻加入狀態提示：
       - `rev > 2.0%`：標註 `🔥 rev>2% (強勢)`（計入強勢多頭評級）。
       - `1.0% <= rev <= 2.0%`：標註 `🟡 rev 1~2% (穩健)`（符合選股基本面但未達強勢加嚴標籤）。
  2. **主頁智慧選股清單標頭與欄位強化**：
     - 表頭動態更新為 `近X月平均營收月成長 rev(%)`，數值下方清晰標示 `rev值`。
  3. **當沖與大戶隔日沖跨分頁整合 rev 標籤**：
     - 在「當沖 6 大指標」與「大戶隔日沖」股票名稱旁增加精緻 `rev:+X%` 標籤，便於多空對照。
  4. **備份與規範維護**：
     - 建立備份資料夾 `backup_20260909_194827`，備份目錄嚴格維持最新與次新共 2 份。

### [2026-09-09 19:33:00] - 修復啟動批次檔 start.bat 換行與編碼格式異常並執行系統啟動
- **異動目的**：修復 `start.bat` 原檔內部分換行遺失、中文編碼異常及指令粘連問題，修正為標準 UTF-8 無 BOM + CRLF 格式，確保雙擊或命令列執行時順暢啟動 MoneyDJ Screener 本機伺服器並自動開啟瀏覽器。
- **異動檔案**：
  - `[修改]` `start.bat`（修復換行與註解指令粘連，補全 CRLF 與延遲指令，確保穩定啟動）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260909_193235/` (最新) 與 `backups/backup_20260908_171600/` (次新)
- **詳細修改內容**：
  1. **修復 start.bat 換行與語法**：
     - 解決原檔案註解與 `start`、`netstat`、`echo` 等指令粘連在同一行造成 cmd.exe 解析報錯的問題。
     - 統一以 Windows 標準 CRLF 換行及無 BOM UTF-8 編碼儲存，並增強非互動模式下延遲等待的相容性。
  2. **備份目錄維護**：
     - 自動建立最新備份 `backup_20260909_193235`，並依規範清理舊版，嚴格維持僅保留最新與次新（共 2 個）資料夾。

### [2026-09-08 17:22:00] - 全系統所有頁面與分頁徹底消除左右拖拉軸 (Zero Horizontal Scrollbars)
- **異動目的**：遵循使用者最新指示：「所有頁面不要有左右軸」，全方位檢查並優化「智慧選股」、「波段手法指標」、「當沖指標」、「大戶隔日沖」四大功能分頁及所有表格、卡片與容器，徹底根絕橫向捲軸與拖拉軸。
- **異動檔案**：
  - `[修改]` `style.css`（全域 `html, body, .app-layout, .main-container, .table-responsive, .tab-view, .results-table-section` 強制 `overflow-x: hidden !important`；表格內邊距與字級緊湊化；隱藏橫向捲軸）
  - `[修改]` `index.html`（優化四大表格表頭文字精簡化，收縮欄寬防溢出）
  - `[修改]` `app.js`（MACD(12) 走勢微圖 SVG 自適應寬度收斂至 125px，避免撐開表格寬度）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_171600/` (最新) 與 `backups/backup_20260908_171000/` (次新)
- **詳細修改內容**：
  1. **全域容器徹底禁止水平滾動**：
     - 在 `style.css` 中，對 `html, body`、`.app-layout`、`.main-container`、`.tab-view`、`.results-table-section` 全面加上 `overflow-x: hidden !important; max-width: 100vw; width: 100%; box-sizing: border-box;`。
     - 頂部導航列 `.navbar` 與四大分頁切換列 `.strategy-tabs-bar` 加入 `flex-wrap: wrap` 與 `max-width: 100vw`，避免多按鈕擠出視窗邊界。
  2. **所有表格容器 (`.table-responsive`) 徹底消除左右軸**：
     - 將原本所有預設包含 `overflow-x: auto` 的 `.table-responsive` 統一重構為 `overflow-x: hidden !important; width: 100%; max-width: 100%;`。
     - 包含 `#viewSwing`（智慧選股）、`#viewWaveStrategy`（波段手法指標）、`#viewDayTrading`（當沖指標）、`#viewOvernight`（大戶隔日沖）四大分頁無一例外，皆保證 0 左右軸。
  3. **表格結構與欄位比例最佳化**：
     - `.stock-table`、`.wave-table`、`.day-trading-table`、`.overnight-table` 單元格 padding 縮減至 `0.45rem 0.22rem ~ 0.35rem`，字體調整為 `0.76rem ~ 0.82rem`。
     - 表頭文字適度精簡（例如：`近20日主力買超(張)` ➜ `主力買超(張)`、`近3月平均營收成長(%)` ➜ `營收成長(%)`、`快速看盤` ➜ `看盤`、`① 週 MACD 狀態` ➜ `①週MACD` 等），大幅消除無謂的欄寬佔用。
     - `.day-trading-table` 啟用 `table-layout: fixed; width: 100%;`，依百分比嚴格限制欄寬，杜絕任何撐爆。
  4. **走勢微圖卡片尺寸緊湊化**：
     - `.macd-sparkline-box` 與 `.dt-sparkline-box` 寬度收斂至 `max-width: 125px;`，內部 SVG 改為自適應向量縮放（`width: 100%`），確保在任何標準解析度螢幕下都能自然容納。
  5. **全域橫向捲軸樣式強制隱藏**：
     - 增加 `::-webkit-scrollbar:horizontal { height: 0px !important; display: none !important; }`，從瀏覽器層面確保橫向拖拉軸完全不顯示。
  6. **備份目錄保留維護**：
     - 每次修改前備份確認，`c:\CodeZone\backups\` 保持嚴格僅留最新（`backup_20260908_171600`）與次新（`backup_20260908_171000`）兩組。

### [2026-09-08 16:56:00] - 備份目錄只留最新與次新兩份、當沖表格重要結論欄位前移、依據明細仿 MACD(12) 走勢微圖、徹底消除畫面左右拖拉軸
- **異動目的**：遵循使用者最新兩項重要指示：
  1. 備份檔案清理：僅保留最新與次新日期的備份資料夾（`backup_20260908_164800` 與 `backup_20260908_162300`），清除舊有歷史備份。
  2. 當沖指標表格欄位重排：「達成指標」、「當沖綜合指引」、「依據明細」往前移至「現價 / 漲跌」後面，讓關鍵作戰決策與得分一目瞭然。
  3. 「依據明細」顯示方式全面參考主頁 MACD(12)：改為精緻多因子能量柱狀微圖卡片，點擊可彈出緊湊一頁式實戰診斷彈窗（`#dtDetailModal`）。
  4. 畫面徹底消除左右拖拉軸：透過表格結構優化、寬度比例重新配比與緊湊間距，實現寬度完全自適應且零橫向捲軸。
- **詳細修改內容**：
  1. **備份檔案清理與規則更新**：
     - 清除 `c:\CodeZone\backups\` 中 7 個較舊備份目錄，僅嚴格保留最新（`backup_20260908_164800`）與次新（`backup_20260908_162300`）兩份。
     - 在 `GEMINI.md` 開發守則中正式載明「備份數量限制：僅保留最新與次新（共 2 個）日期的資料夾，超過時自動清理較舊備份」。
  2. **當沖表格欄位順序前移**：
     - 在 `index.html` 與 `app.js` 中調整欄位順序為：
       `股票` ➜ `現價 / 漲跌` ➜ `達成指標 (空/多)` ➜ `當沖綜合指引` ➜ `依據明細 (仿 MACD)` ➜ `① 均價線` ➜ `② 江波圖` ➜ `③ K線型態` ➜ `④ 內外盤` ➜ `⑤ 差異分析` ➜ `⑥ 主力手法`。
     - 使用者無需向右拖曳即可在首要視角直接看見多空得分、綜合操作評級與明細入口。
  3. **依據明細改採 MACD(12) 走勢微圖風格 & 彈窗互動**：
     - 在 `app.js` 實作 `generateDtDetailSparkline(s, dt)`：單元格內呈現如 MACD(12) 的立體微圖卡片（包含頂部多空狀態燈號標籤、中間帶零軸之 6 大指標多空多因子柱狀進度、底部多/空統計項次與「詳細 ↗」）。
     - 點擊卡片觸發 `openDtDetailModal(code)`，以緊湊一頁式彈窗（`#dtDetailModal`）完整展示 6 大獲利指標診斷依據（2x3 網格卡片）與當沖停損操作 SOP，點擊遮罩背景或按 Esc 鍵即刻關閉。
  4. **徹底消除左右拖拉軸**：
     - 在 `style.css` 中移除導致橫向滾動的多餘寬度與過大 padding，設定 `#viewDayTrading .table-responsive { overflow-x: hidden; width: 100%; }`。
     - 表格列寬度採用百分比自適應配比，標籤文字緊湊化，在所有標準螢幕下均能 100% 完整容納，完全沒有左右拖拉軸。
- **異動檔案**：
  - `[修改]` `GEMINI.md`（規範備份目錄僅保留最新與次新兩份時間戳記資料夾）
  - `[修改]` `index.html`（調整 `#dtTable` 欄位順序、新增 `#dtDetailModal`）
  - `[修改]` `style.css`（當沖表格緊湊自適應排版、消除橫向捲軸、`.dt-sparkline-box` 樣式、`#dtDetailModal` 單頁緊湊彈窗）
  - `[修改]` `app.js`（`renderDayTradingTable` 欄位前移重構、`generateDtDetailSparkline` 仿 MACD 走勢微圖卡片、`openDtDetailModal` 彈窗與 Esc/背景點擊關閉事件）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_164800/` (最新) 與 `backups/backup_20260908_162300/` (次新)

---
- **異動目的**：遵循使用者四大核心需求進行全方位升級：
  1. MACD 圖過大縮小符合一頁模式（零拖拉軸/無滾動條），且點擊非 MACD 圖區域（背景遮罩）或按 Esc 立即關閉並回到智慧選股主頁。
  2. 主頁 (篩選結果) 全面更名為「智慧選股」。
  3. 增加「🌊 波段手法指標」獨立分頁，實時呈現週 MACD 金叉、均線多頭、主力鎖碼、營收成長、量價攻擊與月線停利停損 6 大核心手法指標。
  4. 智慧選股表格第一欄增加可勾選選項「波段」，支援全選與個別勾選，狀態與波段分頁連動。
- **詳細修改內容**：
  1. **MACD 彈窗緊湊一頁模式 & 點擊外部返回主頁**：
     - 在 `style.css` 中重構 `#macdModal .modal-content`：最大寬度 610px、高度上限 92vh 且 `overflow: hidden`，徹底消除垂直拖拉軸與滾動條，完美符合單頁無捲軸顯示。
     - SVG 走勢圖高度收斂為 120px，6 大核心指標數據改為緊湊 1 列橫排（6 欄緊湊標籤），指引卡片精簡為雙欄清晰呈現。
     - 點選彈窗外灰色遮罩背景（`e.target === macdModal`）或點擊關閉按鈕，或鍵盤按下 `Escape`，皆觸發 `closeModalAndGoHome()` 關閉彈窗並切換至「智慧選股」主頁。彈窗底部亦新增「🏠 回到智慧選股主頁」直覺按鈕。
  2. **主頁更名為「智慧選股」**：
     - 分頁導航列標籤更名為「🏠 智慧選股」。
     - 頂部導航列 Brand、懸浮按鈕、表格表頭皆統一正名為「智慧選股」，CSV 匯出檔名亦同步更新為 `MoneyDJ智慧選股結果_YYYYMMDD_HHmmss.csv`。
  3. **新增「波段手法指標」分頁**：
     - 導航列加入「🌊 波段手法指標」第 2 分頁。
     - 頁面包含 6 大手法 Banner（①週 MACD 金叉、②均線多頭排列、③主力連續鎖碼、④營收成長動能、⑤量價攻擊型態、⑥出場停利 SOP）。
     - 摘要統計卡片（波段勾選入選、強勢多頭評級、主力平均買超、平均營收月增）。
     - 實戰檢驗表（包含股票代號名稱、收盤漲跌、①週 MACD 狀態、②均線架構、③主力買超、④營收月增、⑤量價型態、⑥波段操作指引、波段防守點位、MACD(12) 走勢放大按鈕）。
     - 支援關鍵字搜尋、僅顯示勾選標的切換模式、以及匯出波段分析專用 CSV 功能。
  4. **智慧選股表格增加「波段」勾選欄位**：
     - 表格第 1 欄新增 `<input type="checkbox" id="checkAllSwing"> 波段` 全選欄（全表增至 12 欄）。
     - 每檔股票對應可獨立勾選之波段 Checkbox，預設全選 7 檔標的。
     - 勾選或取消勾選時，立即連動更新分頁徽章計數（例如「7 檔波段」）與波段分頁實戰檢驗表展示項目。
- **異動檔案**：
  - `[修改]` `index.html`（四大分頁標籤結構、智慧選股表格第一欄新增波段勾選、波段手法指標全新視圖區塊、彈窗屬性與按鈕）
  - `[修改]` `style.css`（MACD 緊湊一頁樣式、小型核取方塊樣式、波段手法專屬樣式）
  - `[修改]` `app.js`（四大分頁切換邏輯、波段勾選聯動事件、波段指標實戰檢驗表渲染、MACD 彈窗外部點擊返家）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_162300/`

---
- **異動目的**：解決使用者反映「為何跟我在網站上執行結果不同」之問題。
- **原因診斷與比對（依據使用者提供的 MoneyDJ 官方截圖）**：
  1. **條件參數差異（200 張 vs 500 張）**：
     - 使用者在 MoneyDJ 網站所選之條件為「近 20 日券商主力買超大於 **200 張**」（網址帶有 `x@370,a@20,b@200`），符合個股共 **7 檔**（包含 `4908 前鼎`，其主力買超為 272 張）。
     - 本專案原先接收之預設值為 **500 張**（`b@500`），導致買超 272 張的 4908 前鼎被自動過濾，系統僅顯示 6 檔。
  2. **預設排序差異**：
     - MoneyDJ 官方預設是以 **「股票代號（小到大）」** 排序（4908 ➜ 5443 ➜ 6133 ➜ 6224 ➜ 6532 ➜ 6672 ➜ 6829）。
     - 本專案原先以「漲跌幅（高到低）」排序，導致兩邊畫面順序不同。
- **修改內容**：
  1. 將預設主力買超張數全面由 500 改為 **200 張**（`index.html`、`app.js`、`moneydj_bookmarklet.js`）。
  2. 預設排序調整為 `stkCode asc`（股票代號升冪排序），完全對齊 MoneyDJ 原站排版。
  3. `server.ps1` 針對 6672 個股名稱正名為「`騰輝電子-KY`」，避免原站 HTML 截斷顯示為「騰輝電子-K」。
  4. 重啟伺服器後，本機端 7 檔股票數據（收盤、漲跌、漲跌幅、DIF週、MACD週、主力買超、營收成長）與 MoneyDJ 官方 100% 完全精準吻合！
- **異動檔案**：
  - `[修改]` `index.html`（`inputChipVol` 預設值改為 200）
  - `[修改]` `app.js`（`currentSort` 預設 `stkCode asc`、`resetDefaults` 與 `runScreening` 預設張數改為 200）
  - `[修改]` `server.ps1`（6672 股票名稱正規化為 `騰輝電子-KY`）
  - `[修改]` `moneydj_bookmarklet.js`（同步更新至 `b@200`）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_160800/`

---

### [2026-09-08 15:58:00] - 將「主頁 (篩選結果)」設為預設首頁並置入 MACD(12) 核心走勢圖於表格內
- **異動目的**：遵循使用者指示「MACD(12)走勢放在主頁(篩選結果)表格內」：
  1. 將「主頁 (篩選結果)」調整為使用者進入網站時的第一個預設啟動分頁（Active Tab），解決原先預設停留在當沖指標頁導致無法直觀看見篩選主頁之問題。
  2. 將「MACD(12) 走勢圖」欄位置放於主頁（篩選結果）表格核心視角（緊鄰「漲跌幅」之後），確保寬度與列資料完美對齊。
  3. 微圖卡片新增頂部狀態列（青藍色 MACD(12) 與紅色週金叉 ▲ 標籤），視覺效果更專業直觀。
  4. 當沖指標表格亦同步附帶 `📈 MACD(12)` 快速標籤，隨時可一鍵彈窗調閱週線 MACD 走勢圖與深度解讀。
- **異動檔案**：
  - `[修改]` `index.html`（分頁導航列將「🏠 主頁 (篩選結果)」前置為第 1 個分頁並預設 active，調整主體內容區 DOM 結構將主頁選股區前置，表格欄位順序對齊）
  - `[修改]` `app.js`（`currentTab` 預設為 `swing`，`initTabs()` 優先順序調整，`sortAndRenderTable()` 欄位與表頭精準一對一對齊，走勢微圖卡片視覺優化，當沖表追加 MACD 彈窗捷徑）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_155400/`

---

### [2026-09-08 15:48:00] - 新增「回到主頁 (篩選結果)」功能與篩選結果表格顯示「MACD(12) 走勢圖」
- **異動目的**：遵循使用者要求：
  1. 增加可以隨時回到主頁（波段篩選結果）的功能，方便在當沖指標、大戶隔日沖分頁間任意切換回選股主頁。
  2. 在波段篩選結果表格中增加顯示「MACD(12)」技術走勢圖，視覺化呈現 DIF(12, 26) 快線向上突破 MACD(9) 慢線之週線黃金交叉與 OSC 柱狀體擴張動能。
- **異動檔案**：
  - `[修改]` `index.html`（頂部導航列新增 `#btnGoHome`、品牌標題新增點擊跳轉 `#navBrandHome`、右下角新增懸浮按鈕 `#floatingHomeBtn`、篩選表格新增 `MACD(12) 走勢圖` 欄位與彈窗容器 `#macdModal`）
  - `[修改]` `style.css`（新增懸浮按鈕玻璃擬態樣式、MACD 迷你走勢圖 Sparkline、DIF/MACD/OSC 數值標籤、彈窗技術圖表與圖例設計）
  - `[修改]` `app.js`（實作 `goHome()` 導航與自動平滑滾動至篩選表格、SVG `generateMacd12Sparkline()` 動態繪製週線交叉與 OSC 紅柱動畫、`openMacdModal()` 放大檢視完整技術線圖與實戰策略）
  - `[修改]` `CHANGELOG.md`（追加本異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_154204/`
- **詳細修改內容**：
  1. **回到主頁多重入口**：
     - 頂部導航列常駐「🏠 回到主頁 (篩選結果)」按鈕。
     - 系統 Logo 與標題區塊設為可點擊鏈結。
     - 畫面右下角配置智慧懸浮膠囊按鈕，點擊後即刻切換至波段多因子分頁並平滑滾動聚焦至選股清單。
  2. **MACD(12) 走勢圖視覺化**：
     - 表格每列新增獨立 SVG Sparkline，呈現 DIF(12-26) 青藍色快線、MACD(9) 黃橙色慢線、零軸基準線、週黃金交叉突破光環及 OSC 遞增紅柱。
     - 附帶 `DIF`、`MACD`、`OSC` 精準數值標籤。
     - 點擊任一走勢圖即可開啟「MACD(12, 26, 9) 週線技術分析彈窗」，提供 560px 寬版高清圖表、多空關鍵指標儀表板、Stock01 實戰策略指引以及 Yahoo 奇摩 / MoneyDJ 技術線圖直達連結。

---

### [2026-09-08 15:25:00] - 實作三大功能切換（當沖指標、波段指標、大戶隔日沖）與 Stock01 獲利方法核對
- **異動目的**：遵循使用者要求，於 `index.html` 整合 3 個核心功能（當沖指標、波段指標、大戶隔日沖），並依據 `Stock01.pdf` 獲利方法（排除舊主力手法與低成本法），針對篩選個股一一核對 6 大當沖指標並完整呈現於當沖指標網頁。
- **判斷手法規則**：
  - ① 均價線：當日 5分K（跌破均價線放空 / 站上均價線做多）
  - ② 江波圖：當日 5分K（底底低破底放空 / 底底高突破做多）
  - ③ K線型態：當日 5分K（高檔爆量成反壓放空 / 量能支撐守穩做多）
  - ④ 內外盤：當日（內盤賣壓重放空 / 外盤 > 60% 做多）
  - ⑤ 差異分析：當日 5分K（弱於大盤放空 / 抗跌強於大盤做多）
  - ⑥ 主力手法：參考券商分點（近20日主力分點買超累積 + 當日拉高倒貨或護盤）
- **異動檔案**：
  - `[修改]` `server.ps1`（串接 TWSE/TPEX MIS API 即時 5分K 數據，後端自動計算 6 大當沖指標與隔日沖評級）
  - `[修改]` `index.html`（新增頂部三大分頁導航列、當沖 6 大指標診斷表、大戶隔日沖實戰看板）
  - `[修改]` `style.css`（新增分頁切換樣式、當沖診斷藥丸標籤、展開式明細卡片與隔日沖風格設計）
  - `[修改]` `app.js`（實作多分頁即時切換、當沖 6 大指標動態渲染、展開明細抽屜、獨立當沖 CSV 匯出）
  - `[修改]` `CHANGELOG.md`（追加本異動記錄）
- **備份存放目錄**：`backups/backup_20260908_151520/`

---

### [2026-09-08 14:44:00] - 新增當沖核心 5 大指標觀看網站與看盤工具指南至 README.md
- **異動目的**：遵循使用者要求，於 `README.md` 詳細記錄「① 均價線、② 江波圖、③ K線、④ 內外盤、⑤ 差異分析」的看盤網站、軟體與平台操作指南。
- **異動檔案**：
  - `[修改]` `README.md`（新增當沖 5 大指標觀看網站指南章節）
  - `[修改]` `CHANGELOG.md`（追加最新異動紀錄）
- **備份存放目錄**：`backups/backup_20260908_144345/`
- **詳細內容**：
  1. 依據 `Stock01.pdf` 當沖實戰需求，在 `README.md` 整理各指標核心要訣。
  2. 條列各指標的最佳觀看網站與工具平台（包含 Yahoo! 奇摩股市、TradingView、玩股網、MoneyDJ、Goodinfo!、CMoney 籌碼K線、TWSE MIS 官方整合系統、三竹看盤軟體與 XQ 全球贏家），附帶操作功能路徑說明與官方網址。

---

### [2026-09-08 14:38:00] - 建立備份區與異動記錄機制
- **異動目的**：遵循使用者要求，建立專案備份區機制與異動歷程記錄檔。
- **異動檔案**：
  - `[新增]` `backups/backup_20260908_143800/`（基準版本完整備份）
  - `[新增]` `CHANGELOG.md`（修改異動記錄檔）
  - `[新增]` `GEMINI.md`（設定代理人行為規範：每次修改前先備份）
- **備份存放目錄**：`backups/backup_20260908_143800/`
- **詳細內容**：
  1. 建立 `backups/` 備放專用目錄，將現行 7 項核心檔案（`index.html`, `style.css`, `app.js`, `server.ps1`, `start.bat`, `moneydj_bookmarklet.js`, `README.md`）完整歸檔為第一份快照。
  2. 建立本異動記錄檔 `CHANGELOG.md`，並訂定修改 SOP。
  3. 配置專案行為規範，確保後續任何修改皆嚴格執行「先備份、再修改、留記錄」。

---

### [2026-09-08 14:21:00] - Stock01.pdf 當沖 7 大指標分析與核對
- **異動目的**：針對 MoneyDJ 篩選出之 7 檔個股，依據使用者提供之 `Stock01.pdf` 進行指標達成率確認。
- **異動內容**：
  1. 解析 `Stock01.pdf` 當沖 7 大獲利術（均價線、江波圖、K線、內外盤比、差異分析、主力手法、低成本法）。
  2. 連線臺灣期貨交易所（TAIFEX）查驗 7 檔個股是否具備股票期貨（確認 7 檔目前皆無股期）。
  3. 連線 TWSE/TPEX 取得最新分時行情與五檔盤後委買委賣量，完成各指標做多與做空之實況比對與報告。

---

### [2026-09-08 12:41:00] - 初始版本建置完成 (v1.0.0)
- **異動目的**：建置 MoneyDJ 智慧多因子選股與 CSV 匯出系統。
- **異動檔案**：
  - `index.html`：專業金融終端 UI 儀表板，支援動態統計指標、排序、搜尋與參數調整。
  - `style.css`：台股「紅漲綠跌」深色主題樣式庫與玻璃擬態設計。
  - `app.js`：前端篩選、動態排序、搜尋與一鍵下載 UTF-8 BOM CSV（相容 Excel）功能。
  - `server.ps1`：Windows 原生微型 HTTP 伺服器，負責連線 MoneyDJ 並轉碼 Big5。
  - `start.bat`：一鍵啟動腳本，背景執行伺服器並自動在瀏覽器開啟。
  - `moneydj_bookmarklet.js`：MoneyDJ 原站專用自動化書籤 / 油猴腳本。
  - `README.md`：系統操作手冊與使用說明。
